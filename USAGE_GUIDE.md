# دليل الاستخدام السريع 📖

## بدء التشغيل ⚡

### الطريقة الأولى: تشغيل مباشر
1. انقر نقراً مزدوجاً على ملف `run.bat`
2. انتظر حتى يتم تشغيل التطبيق

### الطريقة الثانية: من سطر الأوامر
```bash
flutter run -d windows
```

## بناء التطبيق للإنتاج 🏗️

### الطريقة الأولى: باستخدام الملف الدفعي
1. انقر نقراً مزدوجاً على ملف `build.bat`
2. انتظر حتى يكتمل البناء
3. الملف التنفيذي سيكون في: `build\windows\runner\Release`

### الطريقة الثانية: من سطر الأوامر
```bash
flutter build windows --release
```

## الميزات الأساسية 🎯

### 1. لوحة التحكم
- عرض إحصائيات المبيعات اليومية والشهرية
- رسم بياني للمبيعات الأسبوعية
- تنبيهات المخزون المنخفض
- أكثر المنتجات مبيعاً
- النشاط الأخير

### 2. إدارة المنتجات
**إضافة منتج جديد:**
1. اذهب إلى قسم "المنتجات"
2. اضغط على "إضافة منتج جديد"
3. املأ البيانات المطلوبة:
   - اسم المنتج *
   - الباركود *
   - الفئة *
   - سعر الشراء *
   - سعر البيع *
   - الكمية *
   - الحد الأدنى للمخزون
   - الوصف (اختياري)
4. اضغط "إضافة المنتج"

**تعديل منتج:**
1. اضغط على أيقونة القلم بجانب المنتج
2. عدّل البيانات المطلوبة
3. اضغط "حفظ التعديلات"

**حذف منتج:**
1. اضغط على أيقونة سلة المهملات
2. أكد الحذف

**البحث عن منتج:**
- استخدم مربع البحث في الأعلى
- يمكن البحث بالاسم، الباركود، أو الفئة

### 3. إدارة المبيعات
(قيد التطوير)
- إنشاء فاتورة جديدة
- إضافة منتجات للفاتورة
- حساب الإجمالي والضريبة
- طباعة الفاتورة

### 4. إدارة العملاء
(قيد التطوير)
- إضافة عملاء جدد
- تتبع المديونيات
- عرض سجل المعاملات

### 5. التقارير
(قيد التطوير)
- تقارير المبيعات
- تقارير الأرباح
- تقارير المخزون
- التصدير إلى PDF و Excel

## نصائح الاستخدام 💡

### 1. الوضع الداكن/الفاتح
- اضغط على مفتاح "الوضع الداكن" في الشريط الجانبي
- سيتم حفظ الإعداد تلقائياً

### 2. تنبيهات المخزون
- عند إضافة منتج، حدد "الحد الأدنى للمخزون"
- سيظهر تنبيه تلقائي عند وصول الكمية للحد الأدنى
- يمكن رؤية المنتجات المنخفضة في لوحة التحكم

### 3. حساب هامش الربح
- يتم حسابه تلقائياً: (سعر البيع - سعر الشراء) / سعر الشراء × 100
- يظهر على كل بطاقة منتج

### 4. قاعدة البيانات
- يتم حفظ جميع البيانات محلياً في:
  `Documents\SalesManagementSystem\sales_management.db`
- لأخذ نسخة احتياطية، انسخ هذا الملف

## اختصارات لوحة المفاتيح ⌨️

(سيتم إضافتها في التحديثات القادمة)

## حل المشاكل الشائعة 🔧

### المشكلة: التطبيق لا يعمل
**الحل:**
1. تأكد من تثبيت Flutter بشكل صحيح
2. شغّل: `flutter doctor` للتحقق من الإعداد
3. شغّل: `flutter pub get` لتثبيت المكتبات

### المشكلة: لا تظهر البيانات
**الحل:**
1. تحقق من ملف قاعدة البيانات
2. أعد تشغيل التطبيق
3. جرب إضافة بيانات جديدة

### المشكلة: خطأ في البناء
**الحل:**
1. شغّل: `flutter clean`
2. شغّل: `flutter pub get`
3. شغّل: `flutter build windows --release`

## التحديثات المستقبلية 🚀

- ✅ لوحة التحكم الكاملة
- ✅ إدارة المنتجات الكاملة
- 🔄 إدارة المبيعات الكاملة
- 🔄 إدارة العملاء الكاملة
- 🔄 نظام التقارير
- ⏳ نظام الصلاحيات
- ⏳ دعم الطابعات الحرارية
- ⏳ إشعارات واتساب
- ⏳ النسخ الاحتياطي السحابي

## الدعم الفني 📞

للاستفسارات والدعم:
- افتح Issue على GitHub
- أرسل بريد إلكتروني

---

تم إنشاء هذا النظام بـ ❤️ باستخدام Flutter Desktop
