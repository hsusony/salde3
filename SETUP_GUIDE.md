# ملف الإعداد والتثبيت - Setup Guide

## 🚀 دليل الإعداد السريع

### للمستخدمين (Windows)

#### الطريقة الأسهل ⚡
1. **انقر نقراً مزدوجاً على:**
   ```
   setup.bat
   ```
2. اتبع التعليمات على الشاشة
3. انتظر حتى يكتمل التثبيت
4. اختر "Yes" لتشغيل التطبيق مباشرة

---

### للمطورين (الطريقة اليدوية)

#### المتطلبات الأساسية 📋
- Windows 10 أو أحدث
- Flutter SDK 3.0+
- Git
- Visual Studio 2019+ (C++ Desktop Development)

#### خطوات التثبيت 🔧

**1. تثبيت Flutter:**
```bash
# قم بتحميل Flutter من:
https://docs.flutter.dev/get-started/install/windows

# أضف Flutter إلى PATH
# تحقق من التثبيت:
flutter doctor
```

**2. استنساخ/تحميل المشروع:**
```bash
# إذا كنت تستخدم Git:
git clone [repository-url]
cd de3

# أو فك ضغط الملف المحمل
```

**3. تثبيت المكتبات:**
```bash
# تنظيف المشروع
flutter clean

# تحميل المكتبات
flutter pub get
```

**4. تشغيل التطبيق:**
```bash
# للتطوير:
flutter run -d windows

# أو استخدم:
run.bat
```

**5. بناء للإنتاج:**
```bash
# بناء نسخة الإصدار:
flutter build windows --release

# الملف سيكون في:
build\windows\runner\Release\sales_management_system.exe
```

---

### للأنظمة الأخرى (Linux/Mac)

#### Linux:
```bash
chmod +x setup.sh
./setup.sh
```

#### macOS:
```bash
chmod +x setup.sh
./setup.sh
```

---

## 🔍 فحص المشاكل

### مشكلة: Flutter غير معرف
**الحل:**
```bash
# تحقق من تثبيت Flutter:
where flutter

# إذا لم يظهر، أعد تثبيت Flutter وأضفه إلى PATH
```

### مشكلة: خطأ في المكتبات
**الحل:**
```bash
flutter clean
flutter pub cache repair
flutter pub get
```

### مشكلة: خطأ في البناء
**الحل:**
```bash
# تأكد من تثبيت Visual Studio C++ tools:
flutter doctor -v

# ثم:
flutter clean
flutter pub get
flutter build windows --release
```

---

## ✅ التحقق من نجاح التثبيت

بعد تشغيل `setup.bat`، يجب أن ترى:

```
✅ Flutter مثبت بنجاح
✅ جميع المكتبات محملة
✅ لا توجد أخطاء
```

---

## 🎯 الخطوات التالية

بعد إتمام الإعداد:

1. **تشغيل التطبيق:**
   - انقر على `run.bat`
   - أو: `flutter run -d windows`

2. **إضافة بيانات تجريبية:**
   - اذهب إلى قسم "المنتجات"
   - أضف 5-10 منتجات للاختبار

3. **استكشاف الميزات:**
   - لوحة التحكم
   - إدارة المنتجات
   - البحث والفلترة
   - الثيم الداكن/الفاتح

4. **قراءة التوثيق:**
   - `QUICK_START.md` - للبدء السريع
   - `USAGE_GUIDE.md` - للدليل الكامل
   - `PROJECT_SUMMARY.md` - للمعلومات التقنية

---

## 📦 محتويات المشروع

بعد التثبيت سيكون لديك:

```
de3/
├── setup.bat              ✅ ملف الإعداد (Windows)
├── setup.sh               ✅ ملف الإعداد (Linux/Mac)
├── run.bat                ✅ تشغيل التطبيق
├── build.bat              ✅ بناء التطبيق
├── lib/                   ✅ الكود المصدري
├── assets/                ✅ الموارد
├── windows/               ✅ ملفات Windows
└── docs/                  ✅ التوثيق
```

---

## 🆘 الحصول على المساعدة

إذا واجهت مشاكل:

1. **راجع ملف:** `TROUBLESHOOTING.md`
2. **شغّل:** `flutter doctor -v`
3. **تحقق من:** سجل الأخطاء
4. **افتح:** Issue على GitHub

---

## 🔐 الأمان

- ✅ جميع الملفات آمنة
- ✅ لا يوجد اتصال بالإنترنت (بعد التثبيت)
- ✅ البيانات محفوظة محلياً
- ✅ لا يوجد جمع بيانات

---

## 📞 الدعم الفني

- **البريد الإلكتروني:** [البريد]
- **GitHub Issues:** [الرابط]
- **التوثيق:** جميع ملفات .md

---

**ملاحظة:** التثبيت لمرة واحدة فقط. بعدها استخدم `run.bat` للتشغيل المباشر.

**مدة التثبيت:** 2-5 دقائق (حسب سرعة الإنترنت)

**المساحة المطلوبة:** ~500 MB

---

✨ **مبروك! الآن لديك نظام إدارة مبيعات احترافي جاهز للاستخدام!** ✨
