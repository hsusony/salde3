# 🗄️ ملاحظة مهمة: SQL Server

---

## ⚠️ متطلبات التشغيل

### SQL Server مطلوب:
البرنامج يحتاج إلى **SQL Server 2008 R2 Express** أو أحدث ليعمل بشكل صحيح.

### معلومات الاتصال المطلوبة:
```
Server: .\MorabSQLE
Instance: MorabSQLE
Authentication: SQL Server
Username: sa
Password: 123@a
Collation: Arabic_CI_AS
```

---

## 💡 ملاحظة

✅ **SQL Server يجب أن يكون مثبت مسبقاً** على جهاز المستخدم
✅ البرنامج الحالي لا يتضمن تثبيت SQL Server
✅ على المستخدم تثبيت SQL Server بشكل منفصل قبل استخدام البرنامج

---

## � خطوات التشغيل

1. **تأكد من تثبيت SQL Server**
2. **ثبّت البرنامج** من ملف `SalesManagementSystem_Setup.exe`
3. **شغّل البرنامج**
4. **أدخل معلومات الاتصال** بقاعدة البيانات

---

آخر تحديث: 9 نوفمبر 2025
