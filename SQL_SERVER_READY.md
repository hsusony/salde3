# نظام إدارة المبيعات - SQL Server 2008 Edition

## ✅ الحالة الحالية

**تم إنجازه بنجاح:**
- ✅ REST API جاهز (Node.js + Express + MSSQL)
- ✅ تطبيق Flutter محدّث للاتصال بـ API
- ✅ سكريبتات قاعدة البيانات جاهزة
- ✅ توثيق شامل للنظام

**المتطلبات:**
- ⚠️ SQL Server 2008/2008 R2 (غير مثبت حالياً)

---

## 🚀 البدء السريع

### الخطوة 1: تثبيت وإعداد SQL Server
اتبع الدليل الكامل في ملف: **`COMPLETE_SETUP_GUIDE.md`**

### الخطوة 2: تشغيل النظام
```powershell
# طريقة سريعة - تشغيل كل شيء
.\start_system.bat

# أو يدوياً:
# 1. شغّل API
cd backend
node server.js

# 2. في نافذة أخرى، شغّل التطبيق
flutter run -d windows
```

---

## 📁 هيكل المشروع

```
de3/
├── backend/              # REST API (Node.js)
│   ├── config/          # إعدادات قاعدة البيانات
│   ├── routes/          # مسارات API
│   ├── server.js        # الخادم الرئيسي
│   ├── package.json     # حزم Node.js
│   └── .env            # إعدادات الاتصال
│
├── database/            # سكريبتات SQL Server
│   ├── 00_setup_complete_2008.sql    # إعداد كامل
│   └── setup_database_2008.bat       # تشغيل آلي
│
├── lib/                 # تطبيق Flutter
│   ├── services/       # خدمات الاتصال بـ API
│   ├── models/         # نماذج البيانات
│   ├── screens/        # واجهات التطبيق
│   └── main.dart       # نقطة البداية
│
└── COMPLETE_SETUP_GUIDE.md  # دليل الإعداد الشامل
```

---

## 🔗 نقاط نهاية API

| الوظيفة | الطريقة | المسار |
|---------|---------|--------|
| فحص الاتصال | GET | `/api/health` |
| جميع المنتجات | GET | `/api/products` |
| منتج محدد | GET | `/api/products/:id` |
| إضافة منتج | POST | `/api/products` |
| تحديث منتج | PUT | `/api/products/:id` |
| حذف منتج | DELETE | `/api/products/:id` |
| جميع العملاء | GET | `/api/customers` |
| إضافة عميل | POST | `/api/customers` |
| جميع المبيعات | GET | `/api/sales` |
| إضافة بيع | POST | `/api/sales` |

**عنوان API الأساسي:** `http://localhost:3000`

---

## 🛠️ حل المشاكل

### ❌ API لا يتصل بقاعدة البيانات
```powershell
# تحقق من تشغيل SQL Server
Get-Service MSSQL*

# شغّل الخدمة
Start-Service MSSQL$SQLEXPRESS

# تحقق من إعدادات backend/.env
```

### ❌ تطبيق Flutter لا يتصل بـ API
```dart
// تحقق من عنوان API في:
// lib/services/database_helper.dart
static const String baseUrl = 'http://localhost:3000/api';
```

### ❌ خطأ في تسجيل الدخول لـ SQL Server
راجع قسم "حل المشاكل الشائعة" في `COMPLETE_SETUP_GUIDE.md`

---

## 📚 الملفات المهمة

1. **`COMPLETE_SETUP_GUIDE.md`** - دليل الإعداد الكامل
2. **`backend/.env`** - إعدادات الاتصال بقاعدة البيانات
3. **`backend/README.md`** - توثيق API
4. **`database/00_setup_complete_2008.sql`** - إنشاء قاعدة البيانات
5. **`lib/services/database_helper.dart`** - اتصال Flutter بـ API

---

## 💡 ملاحظات

- **المنفذ 3000**: يستخدمه REST API
- **المنفذ 1433**: يستخدمه SQL Server
- **قاعدة البيانات**: `SalesManagementDB`
- **المستخدم الافتراضي**: `sa`

---

## 📞 المساعدة

للمساعدة التفصيلية، راجع:
- `COMPLETE_SETUP_GUIDE.md` - دليل التثبيت والإعداد
- `SQL_SERVER_SETUP.md` - معلومات SQL Server
- `MIGRATION_TO_SQL_SERVER.md` - تفاصيل الترحيل

---

**الآن النظام جاهز 100% للعمل مع SQL Server 2008!** 🎉
