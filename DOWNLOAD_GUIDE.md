# نظام إدارة المبيعات - دليل التحميل والتثبيت

## 📥 دليل التحميل الكامل

---

## الطريقة الأولى: التثبيت التلقائي (موصى بها) ⚡

### للمستخدمين العاديين:

**خطوة واحدة فقط:**
```
انقر نقراً مزدوجاً على: installer.bat
```

سيقوم البرنامج بكل شيء تلقائياً:
- ✅ فحص النظام
- ✅ فحص Flutter
- ✅ تحميل جميع المكتبات (77 مكتبة)
- ✅ إعداد قاعدة البيانات
- ✅ إنشاء اختصار على سطح المكتب
- ✅ تشغيل التطبيق

**المدة المتوقعة:** 3-5 دقائق

---

## الطريقة الثانية: التثبيت اليدوي (للمطورين) 🔧

### المتطلبات الأساسية:

#### 1. نظام التشغيل
- ✅ Windows 10 أو أحدث
- ✅ 64-bit
- ✅ 500 MB مساحة فارغة

#### 2. Flutter SDK

**تحميل Flutter:**
```
رابط التحميل:
https://docs.flutter.dev/get-started/install/windows
```

**خطوات التثبيت:**

1. **تحميل Flutter:**
   - اذهب إلى: https://flutter.dev
   - حمّل Flutter SDK for Windows
   - أو استخدم الأمر:
   ```powershell
   git clone https://github.com/flutter/flutter.git -b stable C:\flutter
   ```

2. **إضافة Flutter إلى PATH:**
   - افتح: لوحة التحكم → النظام → إعدادات النظام المتقدمة
   - اضغط: متغيرات البيئة
   - في "متغيرات المستخدم"، اختر Path
   - اضغط: تعديل
   - اضغط: جديد
   - أضف: `C:\flutter\bin`
   - احفظ واخرج

3. **التحقق من التثبيت:**
   ```bash
   flutter doctor
   ```

#### 3. Visual Studio (للويندوز فقط)

**مطلوب لبناء تطبيقات سطح المكتب:**

1. حمّل Visual Studio Community (مجاني):
   ```
   https://visualstudio.microsoft.com/downloads/
   ```

2. أثناء التثبيت، اختر:
   - ✅ Desktop development with C++
   - ✅ Windows 10 SDK

#### 4. Git (اختياري)

```
https://git-scm.com/download/win
```

---

## 📋 خطوات التثبيت اليدوي

### الخطوة 1: تحميل المشروع

**من GitHub (إذا كان متاح):**
```bash
git clone [repository-url]
cd de3
```

**أو فك الضغط:**
- فك ضغط الملف المحمّل
- انتقل إلى المجلد

### الخطوة 2: فحص Flutter

```bash
# تحقق من تثبيت Flutter
flutter doctor

# يجب أن ترى:
# [✓] Flutter (Channel stable, ...)
# [✓] Windows Version (...)
# [✓] Visual Studio (...)
```

### الخطوة 3: تثبيت المكتبات

```bash
# نظّف المشروع
flutter clean

# حمّل المكتبات
flutter pub get
```

### الخطوة 4: تشغيل التطبيق

```bash
# للتطوير
flutter run -d windows

# أو استخدم
run.bat
```

### الخطوة 5: بناء للإنتاج (اختياري)

```bash
flutter build windows --release
```

الملف سيكون في:
```
build\windows\runner\Release\sales_management_system.exe
```

---

## 🔍 فحص التثبيت

### تحقق من نجاح التثبيت:

```bash
# 1. فحص Flutter
flutter --version

# 2. فحص الأدوات
flutter doctor -v

# 3. فحص الأجهزة المتاحة
flutter devices

# يجب أن ترى: Windows (desktop)
```

---

## 📦 حجم التحميل

| المكون | الحجم |
|--------|-------|
| Flutter SDK | ~1.5 GB |
| مكتبات المشروع | ~150 MB |
| Visual Studio | ~5 GB (إذا لم يكن مثبت) |
| **المشروع نفسه** | **~50 MB** |

---

## ⏱️ وقت التثبيت

| الخطوة | المدة المتوقعة |
|--------|----------------|
| تحميل Flutter | 5-15 دقيقة |
| تثبيت Visual Studio | 20-40 دقيقة |
| تحميل مكتبات المشروع | 2-5 دقائق |
| **الإجمالي** | **~30-60 دقيقة** |

*(في حالة التثبيت لأول مرة)*

---

## 🌐 متطلبات الإنترنت

### أثناء التثبيت:
- ✅ مطلوب (لتحميل المكتبات)
- السرعة الموصى بها: 5 Mbps أو أعلى

### بعد التثبيت:
- ❌ غير مطلوب
- التطبيق يعمل بشكل كامل offline

---

## 🔧 حل المشاكل الشائعة

### مشكلة 1: Flutter command not found

**السبب:** Flutter غير مضاف إلى PATH

**الحل:**
```bash
# تحقق من مكان Flutter:
where flutter

# إذا لم يظهر شيء:
# أضف C:\flutter\bin إلى PATH
```

### مشكلة 2: فشل تحميل المكتبات

**السبب:** مشكلة في الإنترنت أو cache

**الحل:**
```bash
flutter pub cache repair
flutter clean
flutter pub get
```

### مشكلة 3: خطأ في البناء

**السبب:** Visual Studio غير مثبت صحيحاً

**الحل:**
```bash
# فحص Visual Studio
flutter doctor -v

# يجب أن ترى:
# [✓] Visual Studio - develop for Windows
```

### مشكلة 4: التطبيق بطيء

**السبب:** وضع Debug

**الحل:**
```bash
# استخدم وضع Release:
flutter run --release

# أو ابنِ نسخة release:
flutter build windows --release
```

---

## 📱 نسخ أخرى (قريباً)

### Android/iOS
```bash
# قيد التطوير
flutter build apk --release
flutter build ios --release
```

### Web
```bash
# قيد التطوير
flutter build web --release
```

### Linux/macOS
```bash
# متاح الآن
flutter build linux --release
flutter build macos --release
```

---

## 🎁 ملفات إضافية بعد التثبيت

بعد اكتمال التثبيت، سيكون لديك:

```
de3/
├── installer.bat          ✅ برنامج التحميل
├── setup.bat              ✅ الإعداد السريع
├── run.bat                ✅ تشغيل التطبيق
├── build.bat              ✅ بناء الإصدار
├── README.md              ✅ الدليل الكامل
├── QUICK_START.md         ✅ البدء السريع
├── DOWNLOAD_GUIDE.md      ✅ دليل التحميل (هذا الملف)
└── ...
```

---

## 🆘 الحصول على المساعدة

### إذا واجهت مشاكل:

1. **افحص الأخطاء:**
   ```bash
   flutter doctor -v
   ```

2. **راجع التوثيق:**
   - `README.md`
   - `SETUP_GUIDE.md`
   - `TROUBLESHOOTING.md`

3. **اطلب المساعدة:**
   - افتح Issue على GitHub
   - راسلنا على البريد الإلكتروني
   - انضم للمجتمع

---

## ✅ قائمة التحقق النهائية

قبل البدء، تأكد من:

- [ ] Windows 10/11 مثبت
- [ ] 500 MB مساحة فارغة
- [ ] اتصال إنترنت مستقر
- [ ] صلاحيات المدير (موصى بها)
- [ ] مضاد الفيروسات لا يعيق التثبيت

---

## 🎯 الخلاصة

### للمبتدئين:
```
استخدم: installer.bat
```

### للمطورين:
```
1. ثبّت Flutter
2. شغّل: flutter pub get
3. شغّل: flutter run -d windows
```

---

## 📞 الدعم

**البريد الإلكتروني:** [البريد]  
**GitHub:** [الرابط]  
**التوثيق:** جميع ملفات .md في المشروع

---

**مبروك! 🎉**  
**أنت الآن جاهز لتحميل وتثبيت نظام إدارة المبيعات!**

---

*آخر تحديث: 22 أكتوبر 2025*
